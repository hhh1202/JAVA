spring:
  application:
    name: jwtsecurity_login
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/auth_spring_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: auth_spring_user
    password: qwer1234!
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: false
    properties:
      hibernate:
        format_sql: true
      dialect: org.hibernate.dialect.MySQL8InnoDBDialect
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
server:
  port: 8083
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

myapp:
  jwt:
    secret: thisismyjwtsecretofthisisjavaofgit
    expireAccessToken: 1800000 #30 * 60 * 1000
